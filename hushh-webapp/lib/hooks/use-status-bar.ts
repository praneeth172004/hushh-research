/**
 * @deprecated System bars synchronization is owned by `StatusBarManager`.
 * This hook is retained as a no-op to avoid accidental duplicate runtime controllers.
 */
export function useStatusBar() {
  // Intentionally empty.
}
