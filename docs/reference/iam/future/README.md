# IAM Future Notes

This folder stores time-bound exploration notes and implementation proposals.

Rules:

1. Files here are context only.
2. Canonical source-of-truth remains in the parent `docs/reference/iam/` files.
3. If a future note becomes active policy, copy final decisions into canonical docs in the same PR.
